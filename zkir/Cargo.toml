[package]
name = "midnight-zkir"
version = "3.0.0"
edition = "2024"
license.workspace = true
repository.workspace = true
description = "Zero-knowledge intermediate representation used for Midnight."

[[bin]]
name = "zkir"
path = "src/main.rs"
required-features = ["binary"]

[features]
binary = [
  "dep:log",
  "dep:tracing-subscriber",
  "dep:clap",
  "dep:env_logger",
  "dep:tokio",
  "dep:indicatif",
  "dep:console",
  "dep:futures-executor",
]
proptest = [
  "dep:pastey",
  "dep:proptest",
  "dep:proptest-derive",
  "base-crypto/proptest",
  "transient-crypto/proptest",
  "serialize/proptest",
]

[dependencies]
base-crypto = { version = "1.1.0", package = "midnight-base-crypto" }
serialize = { version = "1.0.0", package = "midnight-serialize" }
transient-crypto = { version = "3.0.0", package = "midnight-transient-crypto", features = [
  "cli",
] }

midnight-curves = { version = "^0.3.1" }
midnight-proofs = { version = "^0.8.1" }
midnight-circuits = { version = "^7.2.2" }
midnight-zk-stdlib = { version = "^2.3.3" }

group = "^0.13.0"
const-hex = "^1.14.0"
serde = { version = "^1.0.0", features = ["derive", "rc"] }
serde_bytes = { version = "^0.11.0" }
serde_json = { version = "^1.0.0" }
serde_repr = "^0.1.0"
anyhow = "^1.0.102"
hex = "^0.4.3"
sha2 = "^0.11.0"
sha3 = "^0.11.0"
# rand 0.9.0 is not yet supported by some of our dependencies, we will
# stick with 0.8.0 for now.
rand = "^0.8.0"
rand_chacha = "^0.3.1"
tracing = { version = "^0.1.41" }

log = { version = "^0.4.28", optional = true }
tracing-subscriber = { version = "^0.3.19", optional = true }
clap = { version = "^4.5.40", features = ["derive"], optional = true }
env_logger = { version = "^0.11.8", optional = true }
tokio = { version = "^1.46.1", features = [
  "rt",
  "macros",
  "rt-multi-thread",
], optional = true }
indicatif = { version = "^0.18.4", optional = true }
console = { version = "^0.16.3", optional = true }
futures-executor = { version = "^0.3.32", optional = true }
num-bigint = { version = "0.4" }
num-traits = "0.2"

pastey = { version = "0.2.1", optional = true }
proptest = { version = "1.4.0", optional = true }
proptest-derive = { version = "0.8", optional = true }


[dev-dependencies]
midnight-zkir = { path = ".", features = ["proptest"] }
actix-rt = "^2.9.0"
